version: '3'
services:
    elastic:
        image: elasticsearch:7.12.0
        ports:
            - "9000:9000"
            - "9200:9200"
            - "9300:9300"
        environment: 
            - "discovery.type=single-node"
        networks:
            search-net:

networks:
    search-net:
        external: true